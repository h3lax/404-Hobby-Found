{% extends 'base.html.twig' %}

{% block title %}Crée ton Event{% endblock %}

{% block body %}
    <h1>Crée ton Event</h1>

    {{ form_start(eventForm) }}
        {{ form_widget(eventForm) }}
        <button class="btn btn-success">Create</button>
    {{ form_end(eventForm) }}

{% endblock %}